server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: C:\Users\Equat\IdeaProjects\friends-n-debts
          default-label: main
          clone-on-start: true

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
